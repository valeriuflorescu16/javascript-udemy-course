/* const country = "Romania";
const continent = "Europe";
let population = 20;
console.log(country);
console.log(continent);
console.log(population);

let isIsland = false;
let language;
console.log(typeof isIsland);
console.log(typeof language);
console.log(typeof country);
console.log(typeof continent);
console.log(typeof population);

language = "Romanian";

let halfPopulation = population / 2;
population++;
let finlandPopulation = 6;
let morePeople = population > finlandPopulation;
let avgPopulation = 33;
let lessThanAverage = population < avgPopulation;
let description = "Portugal is in Europe, and its 11 million people speak Portuguese";

console.log(halfPopulation);
console.log(population);
console.log(finlandPopulation);
console.log(morePeople);
console.log(avgPopulation);
console.log(lessThanAverage);
console.log(description);

let markHeight, johnHeight;
let markMass, johnMass;
let markBMI, johnBMI;
let markHigherBMI;

markMass = 78;
markHeight = 1.69;

johnMass = 92;
johnHeight = 1.95;

markBMI = markMass / (markHeight) ** 2;
johnBMI = johnMass / (johnHeight) ** 2;
markHigherBMI = markBMI > johnBMI;

if (markHigherBMI) {
  console.log(`Mark's BMI (${markBMI}) is higher than John's BMI (${johnBMI}).`);
} else {
  console.log(`John's BMI (${johnBMI}) is higher than Mark's BMI (${markBMI}).`);
}

markMass = 95;
markHeight = 1.88;

johnMass = 85;
johnHeight = 1.76;

markBMI = markMass / (markHeight) ** 2;
johnBMI = johnMass / (johnHeight) ** 2;
markHigherBMI = markBMI > johnBMI;

if (markHigherBMI) {
  console.log(`Mark's BMI (${markBMI}) is higher than John's BMI (${johnBMI}).`);
} else {
  console.log(`John's BMI (${johnBMI}) is higher than Mark's BMI (${markBMI}).`);
}
let d1 = 99;
let d2 = 118;
let d3 = 101;

let k1 = 109;
let k2 = 95;
let k3 = 123;

let dolphinsAverage = (d1 + d2 + d3) / 3;
let koalaAverage = (k1 + k2 + k3) / 3;

if (dolphinsAverage > koalaAverage) {
  console.log(`Dolphins win`);
} else if (dolphinsAverage < koalaAverage) {
  console.log(`Koalas win`);
} else {
  console.log(`it's a draw`);
}

let dolphinsBool = dolphinsAverage >= 100;
let koalaBool = koalaAverage >= 100;

console.log(dolphinsAverage);
console.log(koalaAverage);
console.log(dolphinsBool);
console.log(koalaBool);

if (dolphinsAverage > koalaAverage && dolphinsBool) {
  console.log(`Dolphins win`);
} else if (dolphinsAverage < koalaAverage && koalaBool) {
  console.log(`Koalas win`);
} else if (dolphinsAverage === koalaAverage && dolphinsBool) {
  console.log(`it's a draw`);
} else if (!dolphinsBool && !koalaBool) {
  console.log(`no winner`);
}
*/




